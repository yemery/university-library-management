import { Label, TextInput } from "flowbite-react";

const SearchInput = () => {
  return (
    <div>
      <form action=""></form>
      <TextInput
        id="search"
        placeholder="Search"
        required
        color="gray"
        className=""
      />
    </div>
  );
};

export default SearchInput;
